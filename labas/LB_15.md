# Звіт про Code Review: BadStoreApp 🕵️‍♂️

## Знайдені проблеми

| # | Категорія | Де (файл/рядок) | Проблема | Чому це погано | Як виправити |
|---|----------|------------------|---------|----------------|--------------|
| 1 | Security | `BadStoreApp.java:32, 35` | Логування пароля | Витік конфіденційних даних у логи | Прибрати пароль із `System.out.println` |
| 2 | Security | `BadStoreApp.java:70, 95, 104, 124` | SQL Injection | Можливість несанкціонованого доступу до БД | Використовувати `PreparedStatement` |
| 3 | Maintainability | `BadStoreApp.java:117` | Валідація всередині бізнес-логіки | Перевірка email.trim().length() == 0 змішана з генерацією звіту. | Винести валідацію в окремий метод `Validator.isValidEmail()` або в шар сервісу. |
| 4 | Correctness | `BadStoreApp.java:89` | Hardcoded logic (Число 7) | `total = total - 7`. Що це? | Винести в константу: `private static final int DISCOUNT_FLAT_RATE = 7;` і нормальлно пояснити що це таке. |
| 5 | Architecture | `BadStoreApp.java:79, 99, 110, 132` | Поглинання винятків `(Empty catch)` | `catch (Exception e) {}` — це маскування помилок. Якщо все полетить, то програма просто промовчить. | Логувати помилку: `e.printStackTrace()` або використовувати Logger. |
| 6 | Readability | `BadStoreApp.java:19, 24, 55, 61, 86` | Погані назви змінних `(sc, cmd, qty, rep, p)` | Однолітерні назви або скорочення змушують розробника розшифровувати код у голові. | Перейменувати на `scanner, userCommand, quantity, report, price` |
| 7 | Correctness | `BadStoreApp.java:139` | Off-by-one в циклі | Викличе помилку `IndexOutOfBoundsException` | Змінити `i <= lines.size()` на `i < lines.size()` |
| 8 | Correctness | `BadStoreApp.java:117` | Порівняння String через `==` | Порівнюються посилання, а не значення | Використовувати `.equals()` |
| 9 | Maintainability | `BadStoreApp.java:14` | Hardcoded Credentials | Паролі до БД відкриті для всіх | Винести секрети у змінні оточення або конфіг |
| 10 | Architecture | `BadStoreApp.java:140` | Витік ресурсів | З'єднання з БД не закриваються | Використовувати `try-with-resources` |
